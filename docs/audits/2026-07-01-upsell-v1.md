# Audit upsell v1 (endpoint /scannorder/upsell + construction produit)

> Contenu à restaurer — cet audit (le fix initial sur `/scannorder/{slug}/upsell` : `is_popular`, filtre `is_product_group`, `GetProductFromMerchantId` par produit, cache Redis) a été réalisé lors d'un échange précédent qui n'est pas accessible dans le contexte de cette session. Fournir le contenu original (copier-coller de la conversation) pour le réintégrer ici.
