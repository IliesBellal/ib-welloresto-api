# Audit Cart & Pricing

> Contenu à restaurer — le fichier source `docs/audit-cart-pricing.md` indiqué comme existant n'a pas été trouvé dans le repo (recherché via glob `docs/**/*.md` et `**/*cart*pricing*`, aucun résultat). Merci de vérifier le chemin exact (autre branche ? autre dossier ? jamais committé ?) ou de fournir le contenu directement pour qu'il soit intégré ici.
