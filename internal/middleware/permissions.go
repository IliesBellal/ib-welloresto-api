package middleware

// Chaque constante est une PermissionFunc prête à l'emploi
// Adapte ces fonctions à tes vrais champs booléens
var (
// Exemples de droits courants, à adapter selon ton modèle User
// CanReadReports   PermissionFunc = func(u *auth.User) bool { return u.CanReadReports }
// CanWriteReports  PermissionFunc = func(u *auth.User) bool { return u.CanWriteReports }
// CanManageUsers   PermissionFunc = func(u *auth.User) bool { return u.CanManageUsers }
// CanAccessBilling PermissionFunc = func(u *auth.User) bool { return u.CanAccessBilling }
// ... ajoute tous tes droits ici, un par un
)
